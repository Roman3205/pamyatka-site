<!DOCTYPE html>
<html lang="ru">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Adminka</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/edit.css">
    <script src="/js/auth.js" defer></script>
</head>

<body>
    <div class="container">
        <header class="profile__header d-flex flex-wrap py-3 mb-3">
            <div class="back__link d-flex flex-wrap justify-self-flex-end">
                <a href="/pamyatkaplus/navigation?usermenu={{usermenu._id}}" class="btn btn-secondary active">Назад</a>
            </div>
            <div class="out__link d-flex flex-wrap justify-content-start">
                <a href="/pamyatkaplus/start" class="btn bg-danger active">Выйти из аккаунта</a>
            </div>
        </header>
    </div>
    <main>
        <div class="container products-container">
            <div class="row g-3 products-row">
                <div class="col-12 products-list">
                    <div class="product card mb-3">
                        <div class="row g-0">
                            <div class="col-md-4">
                                <img src="{{usermenu.profile_image}}" width="350px">
                            </div>
                            <div class="col-md-8">
                                <div class="card-body">
                                    <h5 class="card-title mb-3">
                                        Ваш профиль
                                    </h5>
                                    <form action="/pamyatkaplus/profile/edit?usermenu={{usermenu._id}}" method="POST">
                                        <div class="mb-3">
                                            <input name="name" type="text" class="form-control"
                                                placeholder="Имя" value="{{usermenu.name}}">
                                        </div>
                                        <div class="mb-3">
                                            <input name="email" type="email" class="form-control"
                                                placeholder="Ваш email" value="{{usermenu.email}}">
                                        </div>
                                        <div class="mb-3 d-flex">
                                            <input name="password" type="password" class="form-control me-3 input-password"
                                                placeholder="Ваш пароль" value="{{usermenu.password}}">
                                                <button class="btn btn-outline-secondary button-password" style="padding: 4px 5px; margin-left: -5px;">
                                                <img src="/assets/hide.png" class="image-password" width="22px">
                                                </button>
                                        </div>
                                        <div class="mb-3">
                                            Аватарка профиля:
                                            <input name="image" type="text" class="form-control"
                                                placeholder="Ссылка на изображение" value="{{usermenu.profile_image}}">
                                        </div>
                                        <div class="d-flex justify-content-end mb-3">
                                            <button class="btn btn-save px-4 py-2">Сохранить</button>
                                        </div>
                                        {{#if error}}
                                        <div class="alert alert-danger">Произошла ошибка, попробуйте ещё раз</div>
                                        {{/if}}
                                        {{#if success}}
                                        <div class="alert alert-success">Изменения сохранены</div>
                                        {{/if}}
                                    </form>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>
</body>

</html>