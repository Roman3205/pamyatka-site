<!DOCTYPE html>
<html lang="ru">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Adminka</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/edit.css">
</head>

<body>
    <div class="container">
        <header class="d-flex flex-wrap justify-content-end py-3 mb-3">
            <div class="back__link">
                <a href="/adminka?uniqueidentificatoradminkaaccessopenqazwsxedcrfvtgbyhnujmqwertyuiopasdfghjklzxcvbnm=1" class="btn btn-secondary active">Назад</a>
            </div>
        </header>
    </div>
    <main>
        <div class="container products-container">
            <div class="row g-3 products-row">
                <div class="col-12 products-list">
                    <div class="product card mb-3">
                        <div class="row g-0">
                            <div class="col-md-4">
                                <img src="{{task.image}}" width="350px">
                            </div>
                            <div class="col-md-8">
                                <div class="card-body">
                                    <h5 class="card-title">
                                        Редактор задания: {{task.title}}
                                    </h5>
                                    <form action="/adminka/task/edit?id={{task._id}}" method="POST">
                                        <div class="mb-3">
                                            <input name="title" type="text" class="form-control"
                                                placeholder="Название" value="{{task.title}}">
                                        </div>
                                        <select name="category" class="form-select mb-3">
                                            {{{options}}} 
                                        </select>
                                        <div class="mb-3">
                                            <textarea name="description" class="form-control"
                                                placeholder="Описание">{{task.description}}</textarea>
                                        </div>
                                        <div class="mb-3">
                                            <input name="image" type="text" class="form-control"
                                                placeholder="Ссылка на изображение" value="{{task.image}}">
                                        </div>
                                        <div class="mb-3">
                                            <input name="answer" type="text" class="form-control"
                                                placeholder="Ответ на задание" value="{{task.answer}}">
                                        </div>
                                        <div class="form-check mb-3">
                                            <input name="isChecked" class="form-check-input" type="checkbox" id="isChecked" {{#if task.isChecked}}checked{{/if}}>
                                            <label class="form-check-label" for="isChecked">
                                                Одобрить
                                            </label>
                                        </div>
                                        <div class="d-flex justify-content-end mb-3">
                                            <button class="btn btn-save px-4 py-2">Сохранить</button>
                                        </div>
                                        {{#if error}}
                                        <div class="alert alert-danger">Произошла ошибка, попробуйте ещё раз</div>
                                        {{/if}}
                                        {{#if success}}
                                        <div class="alert alert-success">Задание изменено</div>
                                        {{/if}}
                                    </form>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>
</body>

</html>